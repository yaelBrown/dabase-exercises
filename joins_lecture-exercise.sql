JOIN owner_id ON owner_id = owners.id;


-- join left side, start with right side.

-- inner join?
    -- will not show null foreign key
    -- inner joins are common
-- left join?
    -- if value has a null they will display but be null
    -- second most common
-- right join?

-- from (left.table) join (right.table)




